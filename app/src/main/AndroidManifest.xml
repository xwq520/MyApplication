<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xwqaa.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:enabled="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:process=""
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
               android:process=""  指定这个组件运行在哪个进程里
               android:enabled="true" 广播接收器是否能被系统实例化 — “true”可以，“false”不可以。 默认值是“true”。
               < application > 和 < receiver > 的本属性都必须设置为“true”。 只要其中任何一个为“false”，则广播接收器就将被禁用，它将不能被实例化。

              android:exported 广播接收器能否接收来自应用程序之外的消息 — “true”可接收，“false”不接收。
                     如果设为“false”，则本接收器只能接收本应用程序或用户 ID 相同程序的组件所发出的消息。
       -->

        <!-- ATTENTION: This was auto-generated to add Google Play services to your project for
             App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information. -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>
